{% extends 'dashboard/dashboard.html.twig' %}
{% block title %}{{ parent() }} | Information des pièces jointes {% endblock %}
{% block wrapper %}
    <div class="card-header">
        <h2>{{ 'label.titre.informationPj'|trans }}</h2>
    </div>
    <div class="card-body">
        {% include '_flash.html.twig' %}
        <div class="listing-bloc relative-bloc">
            <div class="btn-group wrap-btn">
                <button id="showModalCreate" class="btn btn-success" data-route="information_pj_new" data-title="{{ 'label.title.ajout'|trans }}"><i class="icon-plus-circle"></i><span>Ajouter</span></button>
            </div>
            <div class="table-responsive">
                <table id="listInformationPj" class="table table-striped">
                    <thead>
                        <tr>
                            <th><span>{{ 'column.libelle'|trans }}</span></th>
                            <th><span>{{ 'column.actif'|trans }}</span></th>
                            <th class="actions"><span>{{ 'column.actions'|trans }}</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for information_pj in information_pjs %}
                            <tr>
                                <td>{{  information_pj.libelle }}</td>
                                <td>{{  information_pj.isActif ? 'OUI' : 'NON' }} </td>
                                <td class="actions">
                                    <button data-route="information_pj_edit" data-title="{{ 'label.title.modification'|trans }}" class="btn-edit" data-id="{{ information_pj.id }}" type="button"><i class="icon-edit"></i></button>
                                    <button data-route="information_pj_delete" data-title="{{ 'label.titre.delete'|trans }}" class="btn-remove" data-id="{{ information_pj.id }}" type="button"><i class="icon-trash"></i></button>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        {% include '/Admin/Popup/modal.html.twig' %}
    </div>
{% endblock %}