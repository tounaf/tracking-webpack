{% extends 'dashboard/dashboard.html.twig' %}
{% block title %}{{ parent() }} | Nature de décision {% endblock %}
{% block wrapper %}
    <div class="card-header">
        <h2>{{ 'label.titre.natureDecision'|trans }}</h2>
    </div>
    <div class="card-body">
        {% include '_flash.html.twig' %}
        <div class="listing-bloc relative-bloc">
            <div class="btn-group wrap-btn">
                <button id="showModalCreate" class="btn btn-success" data-route="nature_decision_new" data-title="{{ 'label.title.ajout'|trans }}"> <i class="icon-plus-circle"></i><span>Ajouter</span></button>
            </div>
            <div class="table-responsive">
                <table id="listNatureDecision" class="table table-striped">
                    <thead>
                        <tr>
                            <th><span>{{ 'Nature de décision de justice'|trans }}</span></th>
                            <th><span>{{ 'column.actif'|trans }}</span></th>
                            <th class="actions"><span>{{ 'column.actions'|trans }}</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for nature_decision in nature_decisions %}
                            <tr>
                                <td>{{  nature_decision.libelle }}</td>
                                <td>{{  nature_decision.isActif ? 'OUI' : 'NON' }} </td>
                                <td class="actions">
                                    <button data-route="nature_decision_edit" data-title="{{ 'label.title.modification'|trans }}" class="btn-edit" data-id="{{ nature_decision.id }}" type="button"><i class="icon-edit"></i></button>
                                    <button data-route="nature_decision_delete" data-title="{{ 'label.titre.delete'|trans }}" class="btn-remove" data-id="{{ nature_decision.id }}" type="button"><i class="icon-trash"></i></button>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        {% include '/Admin/Popup/modal.html.twig' %}
    </div>
{% endblock %}