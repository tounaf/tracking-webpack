{% extends 'dashboard/dashboard.html.twig' %}
{% block title %}{{ parent() }} | categorie litige{% endblock %}
{% block wrapper %}
    <div class="card-header">
        <h2>{{ 'label.titre.catLitige'|trans }}</h2>
    </div>
    <div class="card-body">
        {% include '_flash.html.twig' %}
        <div class="listing-bloc relative-bloc">
            <div class="btn-group wrap-btn">
                <button id="showModalCreate" class="btn btn-success" data-route="categorie_litige_new" data-title="{{ 'label.title.ajout'|trans }}"> <i class="icon-plus-circle"></i><span>Ajouter</span></button>
            </div>
            <div class="table-responsive">
                <table id="listCatLitige" class="table table-striped">
                    <thead>
                        <tr>
                            <th><span>{{ 'column.libelle'|trans }}</span></th>
                            <th><span>{{ 'column.actif'|trans }}</span></th>
                            <th class="actions"><span>{{ 'column.actions'|trans }}</span></th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for categorie_litige in categorie_litiges %}
                            <tr>
                                <td>{{  categorie_litige.libelle }}</td>
                                <td>{{  categorie_litige.isActif ? 'OUI' : 'NON' }} </td>
                                <td class="actions">
                                    <button data-route="categorie_litige_edit" data-title="{{ 'label.title.modification'|trans }}" class="btn-edit" data-id="{{ categorie_litige.id }}" type="button"><i class="icon-edit"></i></button>
                                    <button data-route="categorie_litige_delete" data-title="{{ 'label.titre.delete'|trans }}" class="btn-remove" data-id="{{ categorie_litige.id }}" type="button"><i class="icon-trash"></i></button>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
        {% include '/Admin/Popup/modal.html.twig' %}
    </div>
{% endblock %}