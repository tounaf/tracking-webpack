{% extends 'dashboard/dashboard.html.twig' %}

{% set sectionUser = true %}
{% block wrapper %}
    <div class="card-header">
        <h2>{{ 'Dossiers'|trans }}</h2>
    </div>
    <div class="card-body">
        {% include '_flash.html.twig' %}
        {{ include('dossier/form_search.html.twig') }}
        <div class="table-responsive">
            <table id="listDossier" class="table table-striped">
                <thead>
                    <tr>
                        <th class="text-left"><span>Référence du dossier</span></th>
                        <th class="text-left"><span>Nom</span></th>
                        <th class="text-left"><span>Catégorie</span></th>
                        <th class="text-left"><span>Entité</span></th>
                        <th class="text-left"><span>Statut</span></th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}